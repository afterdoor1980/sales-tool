# 销售手册工具箱 — 验收标准框架

> 版本：v1.4（更新：新增类型F（问答库）专属验收；明确定位——除内部通关判定外，同时作为客户/外部用AI结合本手册skill扩展修订内容时的输出校验规范）
> 用途：定义各模块产出物"是否合格"的判断维度，作为通关测试判定依据，也是"导师点评打分表"的设计基础；当客户或使用方以AI结合本手册skill自主扩展/修订内容时，本框架是约束AI输出质量的最小合格线，避免脱离体系生成不达标内容。
> 前置依赖：字段规范文档（类型A-F）

---

## 一、通用验收维度（所有模块产出必须满足，不分类型）

| 维度 | 说明 | 判定方式 |
|---|---|---|
| 字段完整度 | 是否按字段规范（类型A-E）填满所有必填字段，无空缺 | 逐字段检查，缺一项即不合格 |
| 案例贴合度 | 内容是否基于本企业真实业务场景改写，而非照抄案例公司甲/乙的示范原文 | 对比参训企业实际情况 vs 提交内容，判断是否只是换名字的照抄 |
| 可执行性 | 新人拿到这条内容，能否不经额外解释直接照做 | 找一名非撰写人的学员试读，看能否复述出具体动作 |
| 编号与引用一致性 | 涉及跨类型引用的字段（如类型D引用类型A话术编号），编号是否存在且对应准确 | 交叉核对编号 |
| 跨模块组件对齐 | 声明使用跨模块组件（如类型D挂"系统操作指引"）的模块，是否按字段规范对应组件定义提供完整字段（记录节点/触发前置/复用去向），且不写死CRM系统字段名 | 对照字段规范v2.2组件定义逐项核对 |

---

## 二、分类型专属验收维度（叠加在通用维度之上）

| 模板类型 | 专属验收点 |
|---|---|
| 类型A 话术库 | 标准话术与禁忌话术是否形成对照（不能只写该说什么，不写不该说什么）；话术意图是否清楚说明"为什么这么说"；升级路径是否明确（二轮应对方向或转交条件，不能空缺或只写"继续沟通"这类模糊表述）；若话术差异化由多维交叉决定（异议/逼单/竞品/转介绍/催款/客诉等），是否提供维度锚定表作为使用前置导航、能落到具体编号（非多维场景不强制） |
| 类型B 判定标准 | 判定取值是否为可量化/可判断的具体标准，而非"视情况而定"这类模糊表述；每个等级是否都配有对应动作 |
| 类型C 流程清单 | 需先判断子模式：C1（流程型）检查步骤顺序是否合理、责任人和产出物/检查点是否明确；C2（清单目录型）检查条目是否完整覆盖不遗漏，不强求顺序，责任人和产出标准仍需明确，不能是"大家一起跟进"这种无主体表述 |
| 类型D 节奏规则 | 需先判断子模式：D1（周期节奏型）检查触发条件是否具体可判断（如"客户超过X天未回复"而非"感觉客户冷淡了"）；D2（阶梯判定型）检查处理幅度是否给出可执行数字区间、审批权限是否明确到具体层级 |
| 类型E 案例萃取 | 是否提炼出"可复制规律"，而不是停留在"讲了个故事"层面——这是最容易走过场的一类，需要导师重点把关 |
| 类型F 问答库 | 典型问题是否用客户真实口吻（原始表述）而非提炼后的标准问法；统一口径是否与对应正文模块逐条一致（须核对关联模块，正文该模块改动后对应FAQ条目须重新校验）；分类维度是否贴合真实客户提问习惯而非拼凑；涉及红线（合同底线、合规、付款安全）的回答一旦比正文放宽即不合格；是否为穷举式凑数（应只保留代表性/高频问题） |

---

## 三、验收结果分级（用于通关测试判定）

| 等级 | 标准 | 处理方式 |
|---|---|---|
| 合格 | 通用维度+专属维度全部满足 | 通过，纳入手册终稿 |
| 待修改 | 缺1-2项非核心维度（如案例贴合度不足但字段完整） | 现场限时修改后重新提交 |
| 不合格 | 缺失核心维度（如可执行性差、字段大量空缺） | 打回重做，进入下一轮共创环节 |

---

## 四、与后续工具的关系

本框架是"客观标尺"，用于判定产出"过不过关"。下一步的《导师点评打分表》将基于本框架的各维度，转化为导师现场评分时使用的量化打分表（如每个维度打1-5分），并附具体改进意见栏位，两者是同一套判断逻辑的两种呈现形式。

---

## 五、作为AI结合手册skill的生成与扩展校验规范

本框架除用于培训现场的通关判定外，也是客户或使用方**用AI基于本手册skill自主扩展、修订内容时**必须遵守的输出规范。AI生成的模块、条目或话术，在纳入手册前须按本框架自检并满足以下硬约束——任一不满足，视为不合格，退回修订，不带病入库：

| 硬约束 | 要求 |
|---|---|
| 口径一致 | 新增/修订的答案或话术，其口径必须与对应正文模块当前版本一致，不允许自创与正文冲突的新口径 |
| 红线不放宽 | 涉及合同底线、合规、付款安全等问题，不得比正文放宽承诺；凡与正文红线冲突的生成，一律回退并采信正文 |
| 编号引用准确 | 引用的模块/条目编号必须真实存在且指向准确，不得编造编号或"看起来合理"的引用 |
| 案例贴合实际 | 必须基于使用方自身真实业务改写，不得照抄案例公司甲/乙示范原文；复用示范仅作结构参考 |
| 不越权自创 | 对正文未规定边界的场景，不抢先自创口径，应标注"待与模块负责人确认"后再定，而非自行拍板扩大承诺 |
| 类型一致 | 新增模块须先套用对应模板类型（A-F）字段，再填内容；不得为省事跳过类型选型直接自由发挥 |

用法：本框架既可作为随手册skill交付给AI的**输出校验提示**（约束AI在生成/修订时不偏离体系），也可作为客户自检清单在产出入库前过一遍同样的维度。判定方式沿用第三节分级。

---
